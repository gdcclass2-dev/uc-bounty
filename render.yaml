services:
  - type: web
    name: uc-bounty
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: "18"
    disk: 1GB
